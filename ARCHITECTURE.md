# 🏗️ Architecture Overview

## System Architecture (Chunk 1)

```
┌─────────────────────────────────────────────────────────────┐
│                         USER                                 │
│                     (Web Browser)                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP Request
                         │
┌────────────────────────▼────────────────────────────────────┐
│                     FRONTEND                                 │
│                   (Next.js / React)                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  UI Components:                                      │   │
│  │  • VideoFetcher (Form + Results)                     │   │
│  │  • Loading States                                    │   │
│  │  • Error Handling                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  API Service Layer (api.ts):                        │   │
│  │  • videoApi.fetchVideo()                            │   │
│  │  • videoApi.healthCheck()                           │   │
│  └─────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ REST API (JSON)
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      BACKEND                                 │
│                  (FastAPI / Python)                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  API Routes (routes/video_routes.py):               │   │
│  │  • POST /api/v1/fetch_video                         │   │
│  │  • GET  /api/v1/health                              │   │
│  └──────────────────────┬──────────────────────────────┘   │
│                         │                                    │
│  ┌──────────────────────▼──────────────────────────────┐   │
│  │  Models (models/schemas.py):                        │   │
│  │  • VideoFetchRequest                                │   │
│  │  • VideoFetchResponse                               │   │
│  │  • ErrorResponse                                    │   │
│  └──────────────────────┬──────────────────────────────┘   │
│                         │                                    │
│  ┌──────────────────────▼──────────────────────────────┐   │
│  │  Services (services/video_downloader.py):           │   │
│  │  • validate_youtube_url()                           │   │
│  │  • download_video()                                 │   │
│  │  • delete_video()                                   │   │
│  │  • cleanup_old_videos()                             │   │
│  └──────────────────────┬──────────────────────────────┘   │
│                         │                                    │
└─────────────────────────┼────────────────────────────────────┘
                          │
                          │ Downloads video
                          │
┌─────────────────────────▼────────────────────────────────────┐
│                     EXTERNAL SERVICES                         │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                    YouTube                            │   │
│  │              (via yt-dlp library)                     │   │
│  │                                                       │   │
│  │  • Video metadata extraction                         │   │
│  │  • MP4 download                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────┘
                          │
                          │ Saves to
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   LOCAL STORAGE                              │
│                                                              │
│  backend/temp_videos/                                        │
│  └── {uuid}.mp4                                             │
│                                                              │
│  • Temporary storage for downloaded videos                  │
│  • Auto-cleanup after 24 hours                              │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow (Chunk 1)

```
1. User Action
   ┌─────────────────────┐
   │ Paste YouTube URL   │
   │ Click "Fetch Video" │
   └──────────┬──────────┘
              │
2. Frontend Processing
   ┌──────────▼──────────┐
   │ Validate Input      │
   │ Show Loading State  │
   └──────────┬──────────┘
              │
3. API Request
   ┌──────────▼──────────┐
   │ POST /fetch_video   │
   │ {youtube_url: "..."} │
   └──────────┬──────────┘
              │
4. Backend Processing
   ┌──────────▼──────────┐
   │ Validate YouTube URL│
   │ Generate unique ID  │
   │ Call yt-dlp         │
   │ Download MP4        │
   │ Extract metadata    │
   └──────────┬──────────┘
              │
5. Storage
   ┌──────────▼──────────┐
   │ Save to temp_videos/│
   │ {uuid}.mp4          │
   └──────────┬──────────┘
              │
6. Response
   ┌──────────▼──────────┐
   │ Return video info:  │
   │ • Title             │
   │ • Duration          │
   │ • Uploader          │
   │ • Thumbnail         │
   │ • File path         │
   └──────────┬──────────┘
              │
7. Display
   ┌──────────▼──────────┐
   │ Show success message│
   │ Display video card  │
   │ Show metadata       │
   └─────────────────────┘
```

## Technology Stack

### Frontend
```
┌─────────────────────────────┐
│ Next.js 14                  │ Framework
├─────────────────────────────┤
│ React 18                    │ UI Library
├─────────────────────────────┤
│ TypeScript                  │ Type Safety
├─────────────────────────────┤
│ Tailwind CSS                │ Styling
├─────────────────────────────┤
│ Axios                       │ HTTP Client
└─────────────────────────────┘
```

### Backend
```
┌─────────────────────────────┐
│ FastAPI                     │ Web Framework
├─────────────────────────────┤
│ Python 3.8+                 │ Language
├─────────────────────────────┤
│ yt-dlp                      │ Video Downloader
├─────────────────────────────┤
│ Pydantic                    │ Data Validation
├─────────────────────────────┤
│ Uvicorn                     │ ASGI Server
└─────────────────────────────┘
```

## Future Architecture (All Chunks)

```
┌──────────────────────────────────────────────────────────────┐
│                        FULL SYSTEM                            │
│                                                               │
│  Frontend (Next.js)                                          │
│  ↓                                                            │
│  Backend API (FastAPI)                                       │
│  ├── Video Ingestion (Chunk 1) ✅                           │
│  ├── Speech-to-Text (Chunk 2) 🔜                            │
│  ├── Transcript Chunking (Chunk 3) 🔜                       │
│  ├── AI Scoring (Chunk 4) 🔜                                │
│  ├── Clip Generation (Chunk 5) 🔜                           │
│  ├── Vertical Conversion (Chunk 6) 🔜                       │
│  ├── Auto Captions (Chunk 7) 🔜                             │
│  └── Cloud Storage (Chunk 8) 🔜                             │
│  ↓                                                            │
│  External Services                                            │
│  ├── YouTube (yt-dlp)                                        │
│  ├── Hugging Face (Whisper, LLM)                            │
│  ├── FFmpeg (Video Processing)                              │
│  └── Cloud Storage (Supabase/R2)                            │
└──────────────────────────────────────────────────────────────┘
```

## API Endpoints (Current)

```
BASE_URL: http://localhost:8000

┌─────────────────────────────────────────┐
│ POST /api/v1/fetch_video                │
│                                         │
│ Purpose: Download YouTube video         │
│ Auth: None                              │
│ Rate Limit: None (add in production)    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ GET /api/v1/health                      │
│                                         │
│ Purpose: Health check                   │
│ Auth: None                              │
│ Rate Limit: None                        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ GET /docs                               │
│                                         │
│ Purpose: Interactive API documentation  │
│ (Swagger UI)                            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ GET /redoc                              │
│                                         │
│ Purpose: Alternative API documentation  │
│ (ReDoc)                                 │
└─────────────────────────────────────────┘
```

## Security Considerations

### Current (Chunk 1)
- ✅ Input validation (YouTube URL)
- ✅ Error handling
- ⚠️ CORS enabled for all origins (development)
- ⚠️ No rate limiting
- ⚠️ No authentication

### Future Improvements
- 🔜 Add rate limiting
- 🔜 Implement authentication
- 🔜 Restrict CORS to specific origins
- 🔜 Add request validation middleware
- 🔜 Implement file size limits
- 🔜 Add virus scanning
- 🔜 Implement user quotas

## Deployment Architecture

### Development (Current)
```
localhost:3000 (Frontend) → localhost:8000 (Backend)
```

### Production (Future)
```
vercel.app (Frontend) → railway.app (Backend) → supabase.com (Storage)
        ↓                       ↓
   Static Files         Docker Container
   Edge Network         Auto-scaling
```

---

**Last Updated:** December 25, 2025  
**Current Status:** Chunk 1 Architecture Implemented ✅
